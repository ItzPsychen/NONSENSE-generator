package unipd.edids.logicBusiness.strategies.structureStrategies;

/**
 * Represents the types of strategies available for generating or selecting sentence structures.
 *
 * <p>Responsibilities:</p>
 * - Defines the types of structure generation strategies.
 * - Acts as an enumeration for specifying the approach in sentence structure selection.
 */
public enum StrategyType {
    RANDOM,
    SAME,
    SELECTED
}